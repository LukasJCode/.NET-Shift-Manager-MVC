@model IEnumerable<Job>

<h1>JOB MANAGER</h1>

<form class="" method="get" asp-action="Filter">
    <div class="row">
        <div class="col-10">
            <input class="form-control" placeholder="Type to search for jobs..." name="searchString">
        </div>

        <div class="col-2">
            <button class="btn btn-primary" type="submit">Search</button>
        </div>

    </div>
</form>

<div class="mt-2 mb-2">
    <form>
        <button asp-controller="Job" asp-action="Create" class="btn btn-success float-right">Create Task</button>
    </form>
</div>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequiredAge)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var job in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(item => job.Name)
                </td>
                <td>
                    @Html.DisplayFor(item => job.RequiredAge)
                </td>
                <td>
                    <form>
                        <button asp-controller="Job" asp-action="Edit" asp-route-id="@job.Id" class="btn btn-warning">
                            <i class="bi bi-pencil-square"></i>Edit
                        </button>
                        <button asp-controller="Job" asp-action="Delete" asp-route-id="@job.Id" class="btn btn-danger">
                            <i class="bi bi-trash3-fill"></i>Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>